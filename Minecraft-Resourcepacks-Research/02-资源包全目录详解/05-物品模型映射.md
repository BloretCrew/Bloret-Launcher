### 2.5 `assets/<命名空间>/items/` —— 物品模型映射

**功能定位**：1.21.2+ 新增机制，替代原 `models/item/` 的部分作用，直接决定物品使用的模型。

**格式要求**：JSON 文件。

**游戏处理逻辑**：

1. 游戏优先查 `items/`，再回退到 `models/item/`。
2. 支持按组件（components）条件切换模型。

**说明**：样本包中暂未使用此目录，属于较新的机制。此目录在 1.21.2+ 由原版游戏主动使用。

---

