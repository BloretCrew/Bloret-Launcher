### 2.14 特殊文件：`gpu_warnlist.json`

**功能定位**：定义 GPU/渲染器警告规则，当玩家使用不支持的硬件时显示警告。

**格式要求**：

```json
{
  "renderer": ["正则表达式"],
  "version": ["正则表达式"],
  "vendor": ["正则表达式"]
}
```

**游戏处理逻辑**：

1. 启动时检查当前 GPU 渲染器信息。
2. 匹配任意一条正则表达式即弹出对应警告。
3. 资源包可覆盖此文件以添加或修改警告。

**说明**：样本包中未使用，属于原版游戏内置机制。

---

