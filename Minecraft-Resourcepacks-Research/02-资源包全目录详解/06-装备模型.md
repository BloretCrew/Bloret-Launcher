### 2.6 `assets/<命名空间>/equipment/` —— 装备模型

**功能定位**：定义装备（盔甲、马铠等）的纹理映射，控制各个预设模型层使用哪些纹理。

**格式要求**：JSON 文件。

**游戏处理逻辑**：

1. 用于实体装备渲染层的纹理选择。
2. 替代了部分旧版硬编码纹理映射。

**说明**：此目录在样本包中出现较少。原版游戏自 1.21.2+ 开始使用。

---

