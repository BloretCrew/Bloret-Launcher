# Bloret Launcher - Easytier 局域网联机使用指南

## 📖 功能概述

Bloret Launcher 支持基于 Easytier 的局域网联机功能，无需公共服务器，直接通过局域网连接即可一起玩 Minecraft。

## 🎮 联机流程

### 房主（创建游戏的一方）

1. **启动 Easytier**
   - 打开 Bloret Launcher
   - 进入"联机"页面
   - 点击"启动 Easytier"按钮
   - 系统会自动启动 Easytier 服务

2. **查看局域网 IP**
   - 打开命令提示符（Win + R，输入 `cmd`）
   - 运行命令：`ipconfig`
   - 找到"IPv4 地址"，通常是 `192.168.x.x` 或 `10.x.x.x`
   - **将这个 IP 告诉加入者**

3. **启动 Minecraft**
   - 正常启动 Minecraft 游戏
   - 创建局域网世界（打开对局域网开放）
   - 记录下显示的端口号（通常是 `xxxxx`）

4. **等待加入者连接**
   - 保持 Bloret Launcher 运行
   - 加入者连接成功后，你们就可以在同一个虚拟网络中了

### 加入者（加入游戏的一方）

1. **启动 Easytier**
   - 打开 Bloret Launcher
   - 进入"联机"页面
   - 点击"连接到房主的网络"按钮

2. **输入连接信息**
   - **房主用户名**：输入房主的 Bloret PassPort 用户名
   - **联机密钥**：输入房主提供的密钥（如果不知道，询问房主）
   - **房主 IP**：输入房主提供的局域网 IP 地址（如 `192.168.3.168`）

3. **连接成功**
   - 系统会自动连接到房主的网络
   - 连接成功后，会显示 Minecraft 服务器地址
   - 复制这个地址

4. **连接 Minecraft**
   - 打开 Minecraft
   - 添加服务器
   - 粘贴服务器地址
   - 连接游戏

## 🔧 常见问题

### Q: 如何获取我的局域网 IP？

**A**: 
1. 按 `Win + R`，输入 `cmd`，回车
2. 运行命令：`ipconfig`
3. 找到"IPv4 地址"，通常是 `192.168.x.x` 或 `10.x.x.x`

### Q: 联机密钥是什么？

**A**: 
- 联机密钥是 Easytier 网络的密码
- 房主和加入者必须使用相同的密钥才能连接
- 如果房主没有设置特殊密钥，默认是 `NoPassWord`

### Q: 连接失败怎么办？

**A**: 检查以下几点：
1. **确认房主已启动 Easytier**：房主必须先启动 Easytier 服务
2. **确认 IP 地址正确**：房主的局域网 IP 必须是当前的
3. **检查防火墙**：确保防火墙没有阻止 Easytier
4. **同一网络**：确保双方在同一个局域网（同一 WiFi 或路由器）
5. **密钥一致**：双方使用的联机密钥必须相同

### Q: 需要管理员权限吗？

**A**: 
- **不需要！** 新版本的 Easytier 可以直接以普通用户权限运行
- 如果首次启动提示权限错误，请检查安全软件是否阻止了 Easytier

### Q: 可以使用公共服务器吗？

**A**: 
- 当前版本默认使用**本地直连模式**（不依赖公共服务器）
- 如果需要使用公共服务器，可以在代码中修改 `use_public_server=True`
- 但本地直连模式更稳定、延迟更低

## 📊 技术细节

### 工作原理

1. **房主启动**：
   - 启动 `easytier-core.exe` 进程
   - 创建虚拟网络并监听端口（默认 11010）
   - 将连接信息发布到 Bloret PassPort

2. **加入者连接**：
   - 输入房主用户名、密钥和 IP
   - 启动 `easytier-core.exe` 并连接到房主
   - 通过 DHCP 获取虚拟 IP
   - 从 PassPort 获取房主的端口信息

3. **Minecraft 联机**：
   - 使用虚拟 IP 和端口进行游戏
   - 双方在同一个虚拟网络中，可以直连

### 端口说明

- **Easytier 监听端口**：11010 (TCP/UDP), 11011 (WG/WS), 11012 (WSS)
- **Minecraft 端口**：由游戏动态分配，房主创建世界时会显示

## 🛠️ 高级用法

### 手动添加对等节点

如果自动连接失败，可以手动添加：

```bash
# 加入者运行（替换为房主的实际 IP）
easytier\easytier-cli.exe add-peer tcp://房主IP:11010

# 查看连接状态
easytier\easytier-cli.exe node info
```

### 查看日志

日志文件位于：
- `logs/` 目录下
- 查找 `easytier` 相关的日志条目

### 停止 Easytier

- 在 Bloret Launcher 中点击"停止 Easytier"
- 或手动终止进程：`taskkill /F /IM easytier-core.exe`

## 💡 提示

1. **保持 Launcher 运行**：联机过程中不要关闭 Bloret Launcher
2. **可以最小化**：可以关闭联机窗口，Launcher 会在托盘继续运行
3. **端口共享**：房主的 Minecraft 端口对加入者可见
4. **延迟优势**：局域网联机延迟极低，体验接近单机

## 📞 获取帮助

如果遇到问题：
1. 查看日志文件获取详细错误信息
2. 确认双方都已登录 Bloret PassPort
3. 检查网络连接和防火墙设置
4. 联系技术支持

---

**祝游戏愉快！** 🎮✨
